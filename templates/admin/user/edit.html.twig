{% extends 'admin/index.html.twig' %}

{% block title %}ADMIN: {{ user.username }}{% endblock %}

{% block body_content %}
    <div class="container">
        <h1>Editer l'utilisateur :</h1>

{#        {{ include('admin/property/_form.html.twig', {form: form, button: 'Sauvegarder'}) }}#}

        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button class="btn btn-primary" type="submit">{{ button|default('Enregistrer') }}</button>
        {{ form_end(form) }}

        <div class="text-right">
            <a href="{{ path('admin.user.index') }}" class="btn btn-danger">Retour</a>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script>
        $('select').select2();
    </script>
{% endblock %}