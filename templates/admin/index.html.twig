{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="container-fluid mt-4">
        <div class="row">
            <div class="col-md-3 order-md-1">
                <h3 class="mb-2 text-center">ADMINISTRATION</h3>

                <div class="list-group">
                    <a class="list-group-item list-group-item-action {% if current_menu == 'admin.property' %}active{% endif %}" href="{{ path('admin.property.index') }}">Biens</a>
                    <a class="list-group-item list-group-item-action" href="#">Options</a>
                    <a class="list-group-item list-group-item-action {% if current_menu == 'admin.user' %}active{% endif %}" href="{{ path('admin.user.index') }}">Utilisateurs</a>
                </div>
            </div>

            <div class="col-md-9 order-md-2">
                {% block body_content %}{% endblock %}
            </div>
        </div>
    </div>
{% endblock %}
