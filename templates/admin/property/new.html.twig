{% extends 'admin/index.html.twig' %}

{% block title %}ADMIN: {{ property.title }}{% endblock %}

{% block body_content %}
    <div class="container">
        <h1>Nouveau bien:</h1>

        {{ include('admin/property/_form.html.twig', {form: form, button: 'Créer'}) }}

        <div class="text-right">
            <a href="{{ path('admin.property.index') }}" class="btn btn-danger">Retour</a>
        </div>
    </div>
{% endblock %}
